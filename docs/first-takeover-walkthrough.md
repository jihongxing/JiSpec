# First Takeover Walkthrough

Use `docs/takeover-guide.md` and `docs/quickstart.md` instead.

This file stays as a short redirect so old links do not break.

It still points to the main takeover path: `bootstrap discover`, `bootstrap draft`, `adopt`, `spec debt`, and the handoff packet flow.

# Build Agent Prompt

Goal: implement code only after slice artifacts are ready.

Rules:

- respect the approved slice boundary
- do not expand scope without updating the slice artifacts
- reference the slice ID in implementation notes and PR metadata
